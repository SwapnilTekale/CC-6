git config --global user.name "YOUR_USERNAME"
git config --global user.email "YOUR_EMAIL"

git init
git add .
git status 
git commit -m "first commit"
git remote add origin https://github.com/SwapnilTekale/CC-6
git push -u origin main
